1. Palindrome Checker
Write a function that checks if a given string is a palindrome (reads the same forwards and backward).

- Example:
    - Input: `racecar`
    - Output: `True`

2. Factorial Calculator
Create a function that calculates the factorial of a non-negative integer using recursion.

- Example:
    - Input: `5`
    - Output: `120`

3. Fibonacci Sequence Generator  
Write a function that generates the first n numbers of the Fibonacci sequence.

- Example:
    - Input: `8`
    - Output: `[0, 1, 1, 2, 3, 5, 8, 13]`

4. Prime Number Checker  
Create a function to determine if a given number is a prime number.

- Example:
    - Input: `7`
    - Output: `True`

5. File Word Counter  
Write a script that reads a text file and counts the frequency of each word.

- Example:
    - Input: A file containing "hello world hello"
    - Output: `{'hello': 2, 'world': 1}`

6. List Comprehension for Squares  
Use a list comprehension to create a list of the squares of numbers from 1 to 10.

- Example:
    - Output: `[1, 4, 9, 16, 25, 36, 49, 64, 81, 100]`

7. Dictionary Merge  
Merge two dictionaries into one. If there are overlapping keys, the values from the second dictionary should be used.

- Example:
    - Input: `{'a': 1, 'b': 2}`, `{'b': 3, 'c': 4}`
    - Output: `{'a': 1, 'b': 3, 'c': 4}`

8. Find the Missing Number  
Given a list of numbers from 1 to n with one number missing, find the missing number.

- Example:
    - Input: `[1, 2, 4, 5, 6]`
    - Output: `3`

9. Simple Web Scraper  
Use the requests and BeautifulSoup libraries to scrape and print the titles of all the posts on the front page of a news website.

- Example:
    - Output: `['Title 1', 'Title 2', ...]`

10. Basic Class Creation  
Create a Car class with attributes for make, model, and year. Include a method to display the car's information.

- Example:
    - Input: `Car('Toyota', 'Corolla', 2020)`
    - Output: `"2020 Toyota Corolla"`

11. Lambda Function for Sorting  
Use a lambda function to sort a list of tuples based on the second element of each tuple.

- Example:
    - Input: `[('a', 3), ('b', 1), ('c', 2)]`
    - Output: `[('b', 1), ('c', 2), ('a', 3)]`

12. Exception Handling  
Write a function that attempts to divide two numbers and handles the ZeroDivisionError and TypeError exceptions gracefully.

- Example:
    - Input: `divide(10, 0)`
    - Output: `"Cannot divide by zero."`

13. Reverse a String  
Create a function that reverses a given string without using built-in reverse functions.

- Example:
    - Input: `"hello"`
    - Output: `"olleh"`

14. Find Common Elements  
Write a function that returns a list of common elements between two lists.

- Example:
    - Input: `[1, 2, 3]`, `[2, 3, 4]`
    - Output: `[2, 3]`

15. Anagram Checker  
Create a function to check if two strings are anagrams of each other.

- Example:
    - Input: `"listen"`, `"silent"`
    - Output: `True`

16. Decorator for Timing  
Write a decorator that measures and prints the time it takes for a function to execute.

- Example:
    - Output: `"Function executed in 0.002 seconds"`

17. Context Manager for File Handling  
Create a context manager class for opening and closing a file.

- Example:
    - Usage: `with MyFile('test.txt') as f: ...`

18. Multithreading for Downloads  
Write a script that uses multiple threads to download a list of files from the internet concurrently.

- Example:
    - Input: `['url1', 'url2']`
    - Output: `"Downloaded url1"`, `"Downloaded url2"`

19. Basic API Interaction  
Use the requests library to interact with a public API (e.g., the GitHub API) to fetch and display some data.

- Example:
    - Output: `{'login': 'octocat', ...}`

20. Generator for Prime Numbers  
Create a generator function that yields an infinite sequence of prime numbers.

- Example:
    - Output: `2, 3, 5, 7, 11, ...`

21. Implement a Linked List  
Create a Node class and a LinkedList class with methods for adding, removing, and displaying nodes.

- Example:
    - Output: `"1 -> 2 -> 3"`

22. Metaclass for Singleton Pattern  
Implement a metaclass to create a Singleton pattern, ensuring that a class has only one instance.

- Example:
    - Output: `obj1 is obj2  # True`

23. Binary Search Algorithm  
Implement the binary search algorithm to find an element in a sorted list.

- Example:
    - Input: `[1, 2, 3, 4, 5], 4`
    - Output: `3`

24. Sudoku Solver  
Write a program that solves a 9x9 Sudoku puzzle using backtracking.

- Example:
    - Input: `[[5, 3, 0, ...], ...]`
    - Output: `[[5, 3, 4, ...], ...]`

25. Regular Expression for Email Validation  
Write a regular expression to validate an email address.

- Example:
    - Input: `"test@example.com"`
    - Output: `True`

26. Simple Chat Application (Sockets)  
Create a simple command-line chat application using Python's socket module.

- Example:
    - Output: `"User1: Hello!"`

27. Building a REST API with Flask  
Use the Flask framework to build a simple REST API with endpoints for creating, reading, updating, and deleting items.

- Example:
    - Output: `{"id": 1, "name": "Item"}`

28. Asyncio for Concurrent Tasks  
Use the asyncio library to run multiple I/O-bound tasks concurrently.

- Example:
    - Output: `"Task 1 done"`, `"Task 2 done"`

29. Implement a Trie  
Create a TrieNode and Trie class for efficient string searching and prefix matching.

- Example:
    - Input: `insert("apple")`, `search("app")`
    - Output: `True`

30. Image Processing with Pillow  
Write a script that uses the Pillow library to open an image, convert it to grayscale, and save the new image.

- Example:
    - Output: `"grayscale_image.jpg"`

31. JSON File Reader and Writer  
Write functions to read from and write to a JSON file.

- Example:
    - Input: `{"name": "Alice"}`
    - Output: `{"name": "Alice"}`

32. CSV Data Aggregator  
Read a CSV file and calculate the sum of a specific column.

- Example:
    - Input: `data.csv` with column "amount"
    - Output: `Total: 1500`

33. Password Strength Checker  
Create a function that checks the strength of a password based on length and character variety.

- Example:
    - Input: `"P@ssw0rd!"`
    - Output: `"Strong"`

34. Remove Duplicates from List  
Write a function to remove duplicates from a list while preserving order.

- Example:
    - Input: `[1, 2, 2, 3, 1]`
    - Output: `[1, 2, 3]`

35. Flatten a Nested List  
Write a function to flatten a nested list of arbitrary depth.

- Example:
    - Input: `[1, [2, [3, 4]], 5]`
    - Output: `[1, 2, 3, 4, 5]`

36. Count Vowels in a String  
Write a function that counts the number of vowels in a string.

- Example:
    - Input: `"hello world"`
    - Output: `3`

37. Matrix Transpose  
Write a function to transpose a 2D matrix.

- Example:
    - Input: `[[1, 2], [3, 4]]`
    - Output: `[[1, 3], [2, 4]]`

38. Merge Sorted Lists  
Merge two sorted lists into a single sorted list.

- Example:
    - Input: `[1, 3, 5]`, `[2, 4, 6]`
    - Output: `[1, 2, 3, 4, 5, 6]`

39. Find All Subsets  
Write a function to find all subsets of a set.

- Example:
    - Input: `[1, 2]`
    - Output: `[[], [1], [2], [1, 2]]`

40. Most Frequent Element  
Find the most frequent element in a list.

- Example:
    - Input: `[1, 2, 2, 3, 3, 3]`
    - Output: `3`

41. Caesar Cipher Encoder  
Implement a function to encode a string using the Caesar cipher.

- Example:
    - Input: `"abc"`, shift: `2`
    - Output: `"cde"`

42. Palindrome Number Checker  
Check if a given integer is a palindrome.

- Example:
    - Input: `121`
    - Output: `True`

43. GCD and LCM Calculator  
Write functions to compute the greatest common divisor and least common multiple of two numbers.

- Example:
    - Input: `12, 18`
    - Output: `GCD: 6, LCM: 36`

44. Remove Punctuation from String  
Write a function to remove all punctuation from a string.

- Example:
    - Input: `"Hello, world!"`
    - Output: `"Hello world"`

45. Find Longest Word in Sentence  
Write a function to find the longest word in a sentence.

- Example:
    - Input: `"The quick brown fox"`
    - Output: `"quick"`

46. Capitalize Each Word  
Write a function to capitalize the first letter of each word in a string.

- Example:
    - Input: `"hello world"`
    - Output: `"Hello World"`

47. Count Lines in File  
Write a script to count the number of lines in a text file.

- Example:
    - Input: `file.txt`
    - Output: `42`

48. Remove Whitespaces  
Write a function to remove all whitespaces from a string.

- Example:
    - Input: `" a b c "`
    - Output: `"abc"`

49. Find Second Largest Number  
Write a function to find the second largest number in a list.

- Example:
    - Input: `[1, 3, 4, 5, 0, 2]`
    - Output: `4`

50. Convert Roman Numerals to Integer  
Write a function to convert a Roman numeral to an integer.

- Example:
    - Input: `"XIV"`
    - Output: `14`
    - Input: `5`
